import { SavedPropertiesView } from '../../../features/dashboard/components/saved-properties/saved-properties-view'

// Thin per-role wrapper around the shared SavedPropertiesView (see
// v2/features/dashboard/components/saved-properties/saved-properties-view.tsx).
export function SavedPropertiesPageV2() {
  return <SavedPropertiesView />
}
