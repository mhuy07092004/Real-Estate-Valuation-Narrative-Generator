# Enable MSW mock API on production builds (e.g. Vercel demo).
# Mock login: admin@relaive.com / admin
# When the real backend is ready, set to false and redeploy.
VITE_ENABLE_MOCKS=false

# Cloudflare Turnstile site key (public, safe to expose in the client bundle).
VITE_TURNSTILE_SITE_KEY=0x4AAAAAAExRBLdUe2X-hUpS
